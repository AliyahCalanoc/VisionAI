# VisionAI

A React Native (Expo) app that captures a photo, sends it to Google's Gemini Vision API, and displays a structured AI analysis of the scene.

## Setup

1. npm install
2. Create a .env file with EXPO_PUBLIC_GEMINI_KEY=your_key_here
3. npx expo start
4. Scan the QR code with Expo Go
